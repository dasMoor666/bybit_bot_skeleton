# Virtualenv & Python-Cache
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets & lokale Configs
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*secret*
*token*
*apikey*
!.env.example

# Logs / Runs: als Artefakte behandeln (nicht versionieren)
logs/
runs/
*.out
*.err
*.log

# macOS
.DS_Store

# Legacy/Quarantäne (lokal, nicht tracken)
90_LEGACY/

# Root-Artefakte (falls sie auftauchen)
guard.log
autorun.err
autorun.out

# 00_GOV: Backup- und Snapshot-Dateien ignorieren
00_GOV/.gitignore.backup_*
00_GOV/snapshot_*.txt
00_GOV/.gitignore.before_fix_*

# runs: artifacts (keep folder, ignore contents)
 /runs/*
!/runs/.gitkeep
